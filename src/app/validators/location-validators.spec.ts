import { LocationValidators } from './location-validators';

describe('LocationValidators', () => {
  it('should create an instance', () => {
    expect(new LocationValidators()).toBeTruthy();
  });
});
